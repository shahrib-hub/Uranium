const {
  getActiveQuests,
  refreshQuests,
  getQuestRefreshTime,
  claimCompletedQuests
} = require('./service');

module.exports = {
  getActiveQuests,
  refreshQuests,
  getQuestRefreshTime,
  claimCompletedQuests
};
